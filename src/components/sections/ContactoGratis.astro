<section
  class="pt-20 pb-8 px-6 text-center"
  data-aos="fade-up"
  data-aos-duration="800"
  aria-labelledby="contacto-titulo"
>
  <div class="max-w-2xl mx-auto">
    <h2
      id="contacto-titulo"
      class="font-serif text-3xl sm:text-4xl font-semibold mb-2"
    >
      Primera toma de contacto sin cargo
    </h2>

    <p class="text-xl leading-relaxed mb-8">
      Un primer encuentro para conocernos y hablar sobre lo que te trae acá.
    </p>

   
    <a
      href="https://facetime.apple.com/join#v=1&p=CZXm3oOVEfCuziJxTIvfhA&k=wHQq4obS07Ctqj5mrmR-rylQVJhtP06I7XTKTLvmNeA"
      class="block w-fit mx-auto px-8 py-3 bg-[#1d3557] text-white font-medium text-lg rounded-full shadow-lg border-2 border-transparent transition-all duration-300 ease-in-out
             hover:bg-white hover:text-blue-600 hover:border-blue-600
             focus:outline-none focus:ring-4 focus:ring-blue-300
             active:bg-yellow-400 active:text-black active:border-yellow-500 active:shadow-xl mt-[3rem]"
      aria-label="Unirse a la llamada FaceTime"
      rel="noopener noreferrer"
      target="_blank"
    >
      Hablamos ahora
    </a>
  </div>
</section>

<style is:global>
  /* Color principal de tu paleta */
  :root { --principal: #1d3557; }

  .pulse-bottom-glow{
    position: relative;
    display: inline-block;
    color: var(--principal);
    /* “Latido” */
    animation: breathe 3.4s ease-in-out infinite;
    will-change: transform, text-shadow;
  }

  /* Brillo desde abajo */
  .pulse-bottom-glow::before{
    content:"";
    position:absolute;
    left:50%; bottom:-6px;
    transform:translateX(-50%);
    width:120%;
    height:14px;
    pointer-events:none;
    border-radius:9999px;
    /* luz suave con el color del sitio */
    background:
      radial-gradient(50% 120% at 50% 100%,
        rgba(29,53,87,0.35) 0%,
        rgba(29,53,87,0.18) 55%,
        rgba(29,53,87,0) 100%);
    filter: blur(6px);
    animation: glowrise 3.4s ease-in-out infinite;
  }

  /* ligero destello en las letras cuando “late” */
  .pulse-bottom-glow{
    text-shadow:
      0 0 0 rgba(255,255,255,0),
      0 1px 0 rgba(255,255,255,0);
    animation-name: breathe, textglow;
    animation-duration: 3.4s, 3.4s;
    animation-timing-function: ease-in-out, ease-in-out;
    animation-iteration-count: infinite, infinite;
  }

  @keyframes breathe{
    0%,100%{ transform: scale(1); }
    50%{ transform: scale(1.06); }
  }

  /* el brillo de abajo “sube” y se desvanece un poco */
  @keyframes glowrise{
    0%,100%{ opacity:.35; transform:translateX(-50%) translateY(0); }
    50%{ opacity:.6; transform:translateX(-50%) translateY(-3px); }
  }

  /* sutil realce del texto en el pico del latido */
  @keyframes textglow{
    0%,100%{ text-shadow: 0 0 0 rgba(255,255,255,0); }
    50%{ text-shadow: 0 0 12px rgba(255,255,255,.22); }
  }

  /* Respeto a accesibilidad */
  @media (prefers-reduced-motion: reduce){
    .pulse-bottom-glow,
    .pulse-bottom-glow::before{
      animation: none;
    }
  }
</style>

---
import BaseLayout from "../layouts/BaseLayout.astro";
import SobreLaTerapia from "../components/sections/SobreLaTerapia.astro";
import Presentacion from "../components/sections/Presentacion.astro";
import Biografia from "../components/sections/Biografia.astro";
import PorQueElegirme from "../components/sections/PorQueElegirme.astro";
import ContactoGratis from "../components/sections/ContactoGratis.astro";
import FormularioContacto from "../components/sections/FormularioContacto.astro";
---

<BaseLayout>
  <!-- Presentación principal (incluir h1 SEO-friendly dentro del componente) -->
  <Presentacion />

  <!-- Información sobre la terapia -->
  <SobreLaTerapia />

  <!-- Biografía con botón de acción -->
  <Biografia mostrarBoton={true} />

  <!-- Separador Salud Mental -->
  <section
    class="w-full pt-10 pb-[10%] flex items-center justify-center relative overflow-hidden"
    aria-labelledby="salud-mental-heading"
  >
    <div class="flex items-center justify-center text-center px-4">
      <div
        class="flex flex-col items-center justify-center"
        data-aos="fade-up"
        data-aos-duration="1000"
      >
        <h2
          id="salud-mental-heading"
          class="text-2xl sm:text-4xl uppercase font-bebas tracking-widest wave-text"
        >
          La salud física va de la mano de la salud mental.
        </h2>

        <p class="text-xl font-bold sm:text-base mt-4 max-w-xl">
          No sólo somos lo que se ve. Somos <span class="uppercase">cuerpo</span> y <span class="uppercase">mente</span>.
        </p>
      </div>
    </div>
  </section>

  <!-- Por qué elegirme -->
  <PorQueElegirme />

  <!-- CTA de contacto gratuito -->
  <ContactoGratis />

  <!-- Frase destacada -->
  <section
    class="w-full py-16 px-6"
    data-aos="fade-up"
    data-aos-duration="1000"
    aria-labelledby="frase-transformacion"
  >
    <div class="max-w-4xl mx-auto text-center relative">
      <p
        id="frase-transformacion"
        class="text-xl sm:text-2xl leading-relaxed max-w-3xl mx-auto"
      >
        Resulta más sencillo señalar los errores ajenos que indagar, con honestidad, cuánto nos afectan nuestras propias sombras. Renunciar a la idea de perfección y dejar de culpar a otros es clave. El bienestar solo se alcanza dedicando nuestra energía y esfuerzo a transformar lo que está dentro de nosotros mismos.
      </p>
      <div class="w-50 h-1 bg-[#1d3557] mx-auto mt-12"></div>
    </div>
  </section>

  <!-- Formulario de contacto -->
  <FormularioContacto />
</BaseLayout>
